import Chat from "components/chat/Chat";
import DefaultLayout from "components/layouts/DefaultLayout";

const Home = () => {
  return (
    <DefaultLayout>
      <Chat />
    </DefaultLayout>
  );
};

export default Home;
